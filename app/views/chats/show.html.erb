
<%= form_with model: @chat do |f| %>
  <%= f.text_area :message %>
  <%= f.hidden_field :room_id %>
  <%= f.submit %>
<% end %>
<table class="table mt-5">
  <thead>
    <tr class="table-dark">
      <td>投稿者名</td>
      <td>投稿内容</td>
    </tr>
  </thead>
  <tbody>
    <% @chats.each do |chat| %>
      <tr>
        <td><%= chat.user.name %></td>
        <td><%= chat.message %></td>
      </tr>
    <% end %>
  </tbody>
</table>